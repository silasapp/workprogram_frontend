import { GetNoOfJobsOnDeskPipe } from './get-no-of-jobs-on-desk.pipe';

describe('GetNoOfJobsOnDeskPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNoOfJobsOnDeskPipe();
    expect(pipe).toBeTruthy();
  });
});
