import { GetNoOfJobsProcessedPipe } from './get-no-of-jobs-processed.pipe';

describe('GetNoOfJobsProcessedPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNoOfJobsProcessedPipe();
    expect(pipe).toBeTruthy();
  });
});
