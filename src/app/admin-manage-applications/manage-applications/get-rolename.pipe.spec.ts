import { GetRolenamePipe } from './get-rolename.pipe';

describe('GetRolenamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetRolenamePipe();
    expect(pipe).toBeTruthy();
  });
});
